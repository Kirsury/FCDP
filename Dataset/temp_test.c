
    #include <stdio.h>

    // 全局变量
    int global_var = 100;

    // 结构体定义
    struct Point {
        int x;
        int y;
    };

    // 普通函数
    int add(int a, int b) {
    
        return a + b;
    }

    // 带注释的函数
    int subtract(int a, int b) {
    
        // 计算差值
        
        return a - b;
    }
    